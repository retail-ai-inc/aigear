# Google Cloud dependencies
google-cloud-build>=3.15.0
google-cloud-storage>=2.10.0
google-auth>=2.17.0
google-auth-oauthlib>=1.0.0

# YAML support for cloudbuild.yaml
PyYAML>=6.0

# Core aigear dependencies (if not already installed)
# aigear>=0.0.1 